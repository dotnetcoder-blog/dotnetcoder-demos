# DotNetCoder demos

Minimal runnable reproductions for selected DotNetCoder technical articles.

| Demo | Purpose |
| --- | --- |
| [`angular-22-strict-unclaimed-event-names`](./angular-22-strict-unclaimed-event-names/) | Proves that a misspelled Angular output binding fails with `NG8030` when `strictUnclaimedEventNames` is enabled. |

Each demo is intentionally small, independently verifiable, and contains no credentials or external resources.
